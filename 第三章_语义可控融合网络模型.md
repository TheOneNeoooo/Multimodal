# 第三章 面向语义可控的红外与可见光图像融合网络模型

本章针对现有红外与可见光图像融合方法在特征互补与语义控制方面面临的挑战，提出了一种面向语义可控的图像融合网络模型。该模型以Transformer架构为基础，结合CLIP视觉-语言预训练模型，实现了融合过程的语义可控性。本章将详细论述所提网络的核心架构设计、关键技术创新以及损失函数与优化策略。

## 3.2 语义可控融合网络整体架构

本文提出的语义可控图像融合网络在宏观上采用“双流并行编码-交互式融合-语义调制解码”的三阶段流水线架构。其整体网络拓扑结构如图3-1所示。

<figref>图3-1 语义可控红外与可见光图像融合网络整体架构图</figref>

整体网络的工作流程可划分为以下三个核心模块：

**（1）基础特征表示与文本控制信号提取**

网络首先接收处于严格空间配准状态的红外图像与可见光图像对作为输入。针对文本端，本文引入冻结预训练权重的CLIP文本编码器（Text Encoder），接收描述特定任务或退化处理指令的自然语言提示（如“去除弱光噪声并高亮行人”、“增强车辆目标”或“保留建筑细节”），并将其编码为512维的全局语义特征向量。该语义向量作为贯穿整个融合网络的先验控制信号，实现对融合过程的语义调控。

**（2）双流分层特征提取与跨模态交互**

在图像编码阶段，网络摒弃了传统的单流堆叠结构，构建了独立并行的红外与可见光双流分层编码器（Encoder_A和Encoder_B）。通过重叠切片嵌入（Overlap Patch Embedding）与基于无损下采样（PixelUnshuffle）的多尺度构建，网络逐级提取具有多感受野的局部结构信息。在特征聚合的核心区域，网络部署了交互式跨模态注意力模块（Cross-Modal Attention，CR-ATT）。该模块利用注意力机制的交叉查询特性，驱使红外分支与可见光分支在潜在空间中相互校准，深度挖掘两者的互补显著性信息。

**（3）基于语义引导的动态特征调制与重构**

为了将抽象的文本指令转化为物理层面的融合控制力，网络在特征解码与重建阶段引入了基于特征线性调制（Feature-wise Linear Modulation，FiLM）的语义引导机制。经过多层感知机（MLP）降维映射后的文本语义向量，被转化为通道级的缩放因子（Scaling Factor，γ）与偏置因子（Bias Factor，β）。这些动态生成的仿射变换参数直接作用于交互融合后的图像特征通道上，对特定语义响应的特征通道进行自适应的激活或抑制。最终，经过调制的多尺度特征通过上采样与解码器网络，重构出符合文本描述预期的高质量融合图像。

### 3.2.1 网络总体框架

本文所提网络模型的整体框架基于Transformer架构实现，其核心结构参数设置如下：基础通道维度dim=48，四个编码器层级的Transformer块数量分别为[2, 2, 2, 2]，对应的注意力头数为[1, 2, 4, 8]，前馈网络扩展因子ffn_expansion_factor=2。这种分层设计使得网络能够在不同尺度上提取和融合图像特征，同时保持计算效率。

网络的核心模块包括：
- 双流分层编码器（Dual-Stream Hierarchical Encoder）：分别处理可见光图像和红外图像
- 跨模态注意力模块（Cross-Modal Attention）：实现跨模态特征交互
- 全局空间注意力模块（Attention Spatial）：捕捉全局结构信息
- 语义引导调制模块（FeatureWiseAffine）：将文本信息注入图像特征
- 多层级解码器（Decoder Level 1-4）：逐步重构融合图像

## 3.3 双流分层编码器与无损下采样模块设计

本节详细阐述双流分层编码器的设计原理，包括重叠切片嵌入模块和无损下采样模块的具体实现。

### 3.3.1 双流分层编码器总体设计

双流分层编码器采用孪生（Siamese-like）并行结构，包含两个参数独立的特征提取分支：Encoder_A用于处理可见光图像，Encoder_B用于处理红外图像。两个编码器采用完全对称的架构设计，但在训练过程中独立学习各自的特征表示，从而保证对不同模态图像的针对性建模。

整体架构遵循金字塔式（Pyramidal）的分层设计理念，共包含四个阶段（Stage/Level）。随着网络层级的加深，特征图的空间分辨率逐级降低（从H×W降至H/8×W/8），而通道维度逐级倍增（从C增至8C）。这种设计模拟了人类视觉系统的层次化信息处理机制：在浅层保留高分辨率的纹理与边缘细节，在深层捕捉低分辨率的高级语义信息，从而为后续的图像融合与复原任务提供多尺度的特征表示。

以Encoder_A为例，其结构forward过程如下：

```python
# 伪代码展示编码器的前向传播流程
out_enc_level1 = encoder_level1(patch_embed(inp_img))      # 分辨率 H×W, 通道 C
out_enc_level2 = encoder_level2(downsample(out_enc_level1))  # 分辨率 H/2×W/2, 通道 2C
out_enc_level3 = encoder_level3(downsample(out_enc_level2))  # 分辨率 H/4×W/4, 通道 4C
out_enc_level4 = encoder_level4(downsample(out_enc_level3))  # 分辨率 H/8×W/8, 通道 8C
```

编码器最终输出四个尺度的特征图，用于后续的多层级特征融合与图像重建。

### 3.3.2 重叠切片嵌入模块

不同于传统Vision Transformer直接将图像切割为互不重叠的Patch（如16×16），本模型在特征嵌入阶段引入了重叠切片嵌入机制（Overlap Patch Embedding）。该模块的实现代码如下：

```python
class OverlapPatchEmbed(nn.Module):
    def __init__(self, in_c=3, embed_dim=48, bias=False):
        super(OverlapPatchEmbed, self).__init__()
        self.proj = nn.Conv2d(in_c, embed_dim, kernel_size=3, stride=1, padding=1, bias=bias)

    def forward(self, x):
        x = self.proj(x)
        return x
```

该模块采用3×3卷积层（Padding=1, Stride=1）将输入图像映射到高维特征空间。相比于硬性切分，重叠卷积能够有效保持图像局部的连续性，减少切片边界处的伪影。该设计不仅实现了像素到特征的映射，还强化了对底层纹理的捕捉能力，使提取的特征更具局部连续性。

### 3.3.3 无损下采样模块

在特征图尺寸缩小的过程中，传统的池化操作（Pooling）往往会导致有效信息的丢失。为此，本研究在每个Transformer层级之间采用了基于PixelUnshuffle的下采样策略。该模块的实现代码如下：

```python
class Downsample(nn.Module):
    def __init__(self, n_feat):
        super(Downsample, self).__init__()
        self.body = nn.Sequential(
            nn.Conv2d(n_feat, n_feat // 2, kernel_size=3, stride=1, padding=1, bias=False),
            nn.PixelUnshuffle(2))

    def forward(self, x):
        return self.body(x)
```

该模块的工作原理如下：首先利用卷积层将通道数减半，随后通过像素重排操作（PixelUnshuffle），将空间维度上的信息转移至通道维度。这一操作实现了空间分辨率减半的同时通道数翻倍，且在数学上保证了零信息丢失，因此称为“无损下采样”。

相比传统的最大池化或平均池化，PixelUnshuffle能够更好地保留图像的细节信息，避免因空间降采样造成的特征损失。这对于红外与可见光图像融合任务尤为重要，因为两类图像都包含丰富的细节信息需要在特征提取过程中充分保留。

### 3.3.4 Transformer编码层

每个阶段的核心处理单元由若干个Transformer Block堆叠而成。这些模块在保持特征分辨率不变的情况下，利用自注意力机制进行长距离上下文建模，提取当前尺度的特征表示。

Transformer Block的结构包含以下核心组件：

```python
class TransformerBlock(nn.Module):
    def __init__(self, dim, num_heads, ffn_expansion_factor, bias, LayerNorm_type):
        super(TransformerBlock, self).__init__()
        self.norm1 = LayerNorm(dim, LayerNorm_type)
        self.attn = Attention(dim, num_heads, bias)
        self.norm2 = LayerNorm(dim, LayerNorm_type)
        self.ffn = FeedForward(dim, ffn_expansion_factor, bias)

    def forward(self, x):
        x = x + self.attn(self.norm1(x))  # 残差连接 + 自注意力
        x = x + self.ffn(self.norm2(x))   # 残差连接 + 前馈网络
        return x
```

自注意力模块采用多头部注意力机制（Multi-Head Attention），能够同时关注不同位置的信息，实现全局信息的有效整合。前馈网络（Feed Forward Network，FFN）采用门控卷积设计，通过逐点卷积与深度可分离卷积的组合，增强特征的表达能力。

## 3.4 基于跨模态注意力的特征交互与空间融合

为了打破双流网络独立处理的壁垒，实现红外与可见光特征的有效交互与融合，本网络引入了跨模态注意力模块（Cross-Modal Attention，CR-ATT）和全局空间注意力模块（Attention Spatial）。本节将详细推导这两个模块的实现机制。

### 3.4.1 跨模态注意力机制原理

传统双流图像融合方法往往各自独立提取特征，缺乏模态间的信息交互，导致融合结果难以充分利用两种模态的互补特性。针对这一问题，本模块引入了交互式查询（Interactive Querying）策略，通过交换查询向量流向实现跨模态信息交互。

CR-ATT模块的核心创新在于：利用一个模态的查询向量去检索另一个模态的键值向量，从而实现跨模态的特征校准。具体而言：

- **红外流更新**：利用可见光分支的查询向量（Query）去检索红外分支的键值（Key），生成注意力图并作用于红外特征
- **可见光流更新**：利用红外分支的查询向量去检索可见光分支的键值，生成注意力图并作用于可见光特征

这种交叉检索机制使得一个模态能够利用另一个模态的显著性信息（如红外热目标的轮廓或可见光的纹理边界）来校准自身的特征分布，从而实现特征层面的隐式对齐与互补增强。

### 3.4.2 跨模态注意力模块实现

跨模态注意力模块的具体实现代码如下：

```python
class Cross_attention(nn.Module):
    def __init__(self, in_channel, n_head=1, norm_groups=16):
        super().__init__()
        self.n_head = n_head
        self.norm_A = nn.GroupNorm(norm_groups, in_channel)
        self.norm_B = nn.GroupNorm(norm_groups, in_channel)
        self.qkv_A = nn.Conv2d(in_channel, in_channel * 3, 1, bias=False)
        self.out_A = nn.Conv2d(in_channel, in_channel, 1)
        self.qkv_B = nn.Conv2d(in_channel, in_channel * 3, 1, bias=False)
        self.out_B = nn.Conv2d(in_channel, in_channel, 1)
```

该模块首先对输入的红外特征和可见光特征分别进行归一化处理，然后通过卷积层生成各自的查询（Q）、键（K）、值（V）向量。关键在于跨模态注意力的计算方式：

```python
# 红外分支更新：利用可见光的Query检索红外Key-Value
attn_A = torch.einsum("bnchw, bncyx -> bnhwyx", query_B, key_A).contiguous() / math.sqrt(channel)
attn_A = torch.softmax(attn_A, -1)
out_A = torch.einsum("bnhwyx, bncyx -> bnchw", attn_A, value_A).contiguous()
out_A = self.out_A(out_A.view(batch, channel, height, width))
out_A = out_A + x_A  # 残差连接

# 可见光分支更新：利用红外的Query检索可见光Key-Value
attn_B = torch.einsum("bnchw, bncyx -> bnhwyx", query_A, key_B).contiguous() / math.sqrt(channel)
attn_B = torch.softmax(attn_B, -1)
out_B = torch.einsum("bnhwyx, bncyx -> bnchw", attn_B, value_B).contiguous()
out_B = self.out_B(out_B.view(batch, channel, height, width))
out_B = out_B + x_B  # 残差连接
```

该实现通过爱因斯坦求和约定（einsum）高效地计算跨模态注意力图。注意力权重通过softmax函数进行归一化，确保所有位置的注意力权重之和为1。最终输出通过残差连接与输入相加，保证梯度流动的稳定性。

### 3.4.3 全局空间注意力模块

为了弥补卷积操作感受野受限（Local Receptive Field）的不足，本研究在跨模态融合后引入了全局空间注意力模块（Attention Spatial）。该模块基于标准的自注意力机制，计算特征图上任意两个像素点之间的相似度矩阵：

```python
class Attention_spatial(nn.Module):
    def __init__(self, in_channel, n_head=1, norm_groups=16):
        super().__init__()
        self.n_head = n_head
        self.norm = nn.GroupNorm(norm_groups, in_channel)
        self.qkv = nn.Conv2d(in_channel, in_channel * 3, 1, bias=False)
        self.out = nn.Conv2d(in_channel, in_channel, 1)

    def forward(self, input):
        batch, channel, height, width = input.shape
        norm = self.norm(input)
        qkv = self.qkv(norm).view(batch, self.n_head, channel // self.n_head * 3, height, width)
        query, key, value = qkv.chunk(3, dim=2)
        attn = torch.einsum("bnchw, bncyx -> bnhwyx", query, key).contiguous() / math.sqrt(channel)
        attn = torch.softmax(attn, -1)
        out = torch.einsum("bnhwyx, bncyx -> bnchw", attn, value).contiguous()
        out = self.out(out.view(batch, channel, height, width))
        return out + input  # 残差连接
```

通过建立像素级的长距离依赖（Long-range Dependency），该模块能够参考整张图像的上下文信息来修复局部细节。这对于保持融合图像在雨雾遮挡或复杂光照条件下的整体结构一致性至关重要。

### 3.4.4 自适应特征融合模块

特征融合模块负责将跨模态交互后的双流特征整合为统一的多模态表征。该模块首先在通道维度上对双流特征进行拼接（Concatenation），随后通过一个参数可学习的1×1卷积层进行降维投影：

```python
class Fusion_Embed(nn.Module):
    def __init__(self, embed_dim, bias=False):
        super(Fusion_Embed, self).__init__()
        self.fusion_proj = nn.Conv2d(embed_dim * 2, embed_dim, kernel_size=1, stride=1, bias=bias)

    def forward(self, x_A, x_B):
        x = torch.concat([x_A, x_B], dim=1)  # 通道维度拼接
        x = self.fusion_proj(x)  # 1×1卷积降维
        return x
```

1×1卷积本质上起到了“软选择”的作用。它通过反向传播自动学习每个空间位置上不同模态的权重分配（例如，在暗光区域赋予红外特征更高的权重），从而自适应地滤除冗余信息，生成高密度的融合特征。

## 3.5 基于MLP与FiLM的语义引导机制

传统的红外-可见光图像融合方法主要基于像素或特征的简单加权，缺乏对高层语义信息的建模，因而在不同任务场景下往往难以实现目标增强或信息突出。本模型利用预训练的大规模视觉-语言模型（Vision-Language Model）——CLIP作为文本理解的基座，将抽象的自然语言指令转化为机器可理解的控制信号，实现融合过程的语义可控性。

### 3.5.1 CLIP文本特征提取

CLIP（Contrastive Language-Image Pre-training）是由OpenAI提出的大规模视觉-语言预训练模型，通过在海量图文对数据上进行对比学习训练，实现了图像与自然语言的共享语义空间。本模型采用冻结参数的CLIP文本编码器（Text Encoder）将自然语言提示转换为语义向量。

网络首先接收描述具体融合任务的文本提示作为输入。典型的文本提示包括：
- “enhance the vehicles and pedestrians”（增强车辆和行人）
- “remove low-light noise while preserving building details”（去除弱光噪声同时保留建筑细节）
- “highlight thermal targets in the scene”（高亮场景中的热目标）
- “improve contrast and reduce blur”（提高对比度并减少模糊）

这些自然语言指令通过CLIP文本编码器被映射为512维的语义向量：

```python
@torch.no_grad()
def get_text_feature(self, text):
    text_feature = self.model_clip.encode_text(text)
    return text_feature
```

冻结CLIP参数可以保证文本编码器提供的语义特征保持稳定，避免在训练过程中发生语义漂移。同时，由于CLIP在大规模数据上进行了预训练，其编码的语义向量具有良好的泛化能力，能够理解多样化的自然语言指令。

### 3.5.2 语义空间映射

由于CLIP的特征空间与本模型中图像解码器的特征空间存在分布差异，直接使用原始文本向量进行控制效果不佳。因此，本研究设计了多层感知机（MLP）作为语义空间映射适配器：

```python
class FeatureWiseAffine(nn.Module):
    def __init__(self, in_channels, out_channels, use_affine_level=True):
        super(FeatureWiseAffine, self).__init__()
        self.use_affine_level = use_affine_level
        self.MLP = nn.Sequential(
            nn.Linear(in_channels, in_channels * 2),
            nn.LeakyReLU(),
            nn.Linear(in_channels * 2, out_channels * (1 + self.use_affine_level))
        )
```

MLP适配器包含两个全连接层和LeakyReLU激活函数。它将通用的CLIP语义向量（512维）映射到特定于融合任务的潜在空间。MLP的输出维度被调整为与图像特征通道数相匹配（或其倍数，用于生成两组参数），从而确保文本信息能够点对点地作用于图像特征通道。

### 3.5.3 FiLM特征线性调制机制

在获取了对齐后的文本语义向量后，模型通过特征线性调制（Feature-wise Linear Modulation，FiLM）机制，将文本信息“注入”到图像特征中。FiLM模块的数学表达如下：

给定图像特征$x$和文本语义向量$t$，首先通过MLP生成控制参数：

$$[\gamma, \beta] = \text{MLP}(t)$$

其中$\gamma \in \mathbb{R}^C$为缩放因子（Scaling Factor），$\beta \in \mathbb{R}^C$为偏置因子（Shift Factor），$C$为特征通道数。

随后进行仿射变换：

$$\hat{x} = (1 + \gamma) \odot x + \beta$$

其中$\odot$表示逐元素乘法。通过乘法操作调整图像特征的方差（即特征的显著性），通过加法操作调整图像特征的均值（即特征的背景强度）。

该实现代码为：

```python
def forward(self, x, text_embed):
    text_embed = text_embed.unsqueeze(1)
    batch = x.shape[0]
    if self.use_affine_level:
        gamma, beta = self.MLP(text_embed).view(batch, -1, 1, 1).chunk(2, dim=1)
        x = (1 + gamma) * x + beta
    return x
```

具体而言：
- **缩放因子γ的作用**：当γ>0时，对应通道的特征被增强；当γ<0时，对应通道的特征被抑制。通过调整不同通道的响应强度，可以实现对特定语义内容的选择性增强。
- **偏置因子β的作用**：调整特征的基准响应值，改变特征的激活阈值，从而改变特征图的整体亮度和对比度。

为了保证训练的稳定性并保留原始图像的底层细节，调制后的特征通常会通过残差连接与原始特征相加，形成最终送入Transformer解码块的融合特征。

### 3.5.4 多层级语义引导策略

本文采用多层级语义引导策略，在网络的四个解码层级（Level 1-4）均部署了FiLM模块：

```python
self.prompt_guidance_4 = FeatureWiseAffine(in_channels=512, out_channels=dim * 2 ** 3)
self.prompt_guidance_3 = FeatureWiseAffine(in_channels=512, out_channels=dim * 2 ** 2)
self.prompt_guidance_2 = FeatureWiseAffine(in_channels=512, out_channels=dim * 2 ** 1)
self.prompt_guidance_1 = FeatureWiseAffine(in_channels=512, out_channels=dim)
```

不同层级的语义引导具有不同的作用：

- **高层语义引导（Level 3-4）**：作用于低分辨率特征，主要影响图像的整体语义结构和目标显著性
- **低层语义引导（Level 1-2）**：作用于高分辨率特征，主要影响图像的局部纹理和细节

通过在多个层级引入语义引导，网络能够实现粗粒度到细粒度的语义控制，使用户的自然语言指令能够多层次地影响融合结果。

## 3.6 网络损失函数设计与联合优化策略

为了训练本文提出的语义可控融合网络，本节详细阐述所设计的损失函数和优化策略。

### 3.6.1 总体损失函数设计

本文采用多损失函数联合优化策略，总损失函数由以下四个部分组成：

$$\mathcal{L}_{total} = \lambda_{max}\mathcal{L}_{max} + \lambda_{ssim}\mathcal{L}_{ssim} + \lambda_{color}\mathcal{L}_{color} + \lambda_{grad}\mathcal{L}_{grad}$$

其中，各损失分量的含义和作用如下：

**（1）强度损失（Intensity Loss）**

强度损失用于确保融合图像在亮度层面保持与源图像的一致性：

$$\mathcal{L}_{max} = \mathbb{E}[|I_{fused} - I_{target}}|]$$

其中$I_{target}$通过以下方式计算：对于每个像素点，取红外图像和可见光图像中亮度值较大的那个：

$$I_{target} = \mathbb{I}(I_{ir} > I_{vis}) \odot I_{ir} + \mathbb{I}(I_{ir} \leq I_{vis}) \odot I_{vis}$$

该损失确保融合图像能够保留两幅源图像中最显著的信息。

**（2）结构相似性损失（SSIM Loss）**

结构相似性损失用于保持融合图像与源图像在结构层面的相似性：

$$\mathcal{L}_{ssim} = \mathcal{L}_{ssim}(I_{vis}, I_{fused}) + \mathcal{L}_{ssim}(I_{ir}, I_{fused})$$

SSIM损失通过计算亮度、对比度和结构三个维度的相似性，确保融合图像既保留可见光的纹理细节，又保留红外的显著目标信息。

**（3）色彩损失（Color Loss）**

色彩损失用于保持融合图像与可见光图像在色彩层面的一致性：

$$\mathcal{L}_{color} = \mathbb{E}[|C_{vis} - C_{fused}|_1]$$

其中$C$表示图像的色度分量（Cb和Cr）。该损失确保融合图像保持自然的色彩表现，避免因过度增强导致的色彩失真。

**（4）梯度损失（Gradient Loss）**

梯度损失用于保持融合图像的边缘和纹理细节：

$$\mathcal{L}_{grad} = \mathbb{E}[|\nabla I_{fused} - \max(\nabla I_{vis}, \nabla I_{ir})|_1]$$

其中$\nabla$表示使用Sobel算子计算的图像梯度。该损失确保融合图像保留清晰的边缘结构。

### 3.6.2 多任务差异化损失权重

针对不同的退化场景，本文设计了差异化的损失权重配置，以适应不同任务的需求：

| 任务类型 | λ_max | λ_ssim | λ_color | λ_grad |
|---------|-------|--------|---------|--------|
| 低光增强 | 8 | 1 | 12 | 10 |
| 过曝恢复 | 4 | 0 | 12 | 2 |
| 红外低对比度 | 8 | 1 | 12 | 10 |
| 红外去噪 | 6 | 1 | 12 | 10 |

- **低光增强任务**：侧重于强度损失和梯度损失，以确保暗区细节的恢复
- **过曝恢复任务**：降低结构相似性损失权重，避免过度约束导致高光区域无法恢复
- **红外去噪任务**：加强梯度损失以保持边缘信息

### 3.6.3 训练优化策略

本文采用以下训练优化策略：

**（1）优化器选择**：使用AdamW优化器，初始学习率设置为1e-4，权重衰减为5e-2。AdamW优化器结合了Adam的自适应学习率和Weight Decay的正则化效果，能够有效防止模型过拟合。

**（2）学习率调度**：采用带有Warmup策略的学习率调度器。在训练初期，学习率从较小的值逐渐增加，帮助模型稳定收敛；在训练后期，使用余弦退火策略逐渐降低学习率，使模型精细化收敛。

**（3）数据增强**：训练过程中采用随机裁剪（96×96）、水平翻转、垂直翻转等数据增强策略，提高模型的泛化能力。

**（4）模型并行**：支持多GPU数据并行训练，通过PyTorch的DistributedDataParallel或DataParallel模块实现高效训练。

## 3.7 本章小结

本章详细阐述了面向语义可控的红外与可见光图像融合网络模型的核心设计与实现。主要贡献和创新点包括：

**（1）双流分层编码器设计**：采用孪生并行结构分别处理红外与可见光图像，通过重叠切片嵌入和基于PixelUnshuffle的无损下采样模块，在保持细节的同时实现高效的多尺度特征提取。

**（2）跨模态注意力机制（CR-ATT）**：通过交互式查询策略，实现红外与可见光特征在潜在空间中的相互校准与互补增强。全局空间注意力模块进一步捕捉图像的全局结构信息。

**（3）基于MLP与FiLM的语义引导机制**：利用冻结的CLIP文本编码器提取自然语言指令的语义特征，通过MLP适配器进行空间映射，最终通过FiLM机制将语义信息转化为通道级的控制参数，实现融合过程的可控调节。

**（4）多层级语义引导策略**：在网络的四个解码层级均部署语义引导模块，实现从粗粒度到细粒度的多层次语义控制。

**（5）多损失联合优化**：设计包含强度损失、结构相似性损失、色彩损失和梯度损失的复合损失函数，并针对不同退化场景采用差异化的损失权重配置。

本章提出的网络架构为后续章节的实验验证提供了完整的模型基础。通过上述创新设计，网络能够根据自然语言指令动态调整融合策略，实现真正意义上的语义可控图像融合。
