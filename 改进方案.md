# Text-IF 代码改进方案

## 一、创新点说明

基于Text-IF原始代码，你的论文核心创新是：
- **新增：掩码引导的语义区域损失 (Mask-guided Semantic Loss)**
- 其他模块（跨模态注意力、FiLM等）已经实现，无需修改

## 二、需要新增/修改的内容

### 1. 新增损失函数 (scripts/losses.py)

#### 1.1 MaskedSemanticLoss - 掩码引导语义区域损失

```python
class MaskedSemanticLoss(nn.Module):
    """基于掩码引导的语义区域损失 - 论文3.5.1.1

    目标区域：融合图趋近红外图像（高亮热源）
    背景区域：融合图趋近可见光图像（保留纹理）
    """
    def __init__(self):
        super(MaskedSemanticLoss, self).__init__()

    def forward(self, fused_img, ir_img, vis_img, target_mask):
        # target_mask: 目标区域为1，背景为0
        target_mask = target_mask.unsqueeze(1)  # [B,1,H,W]
        background_mask = 1 - target_mask

        # 目标区域损失：融合图趋近红外图像
        loss_target = torch.mean(target_mask * torch.abs(fused_img - ir_img))

        # 背景区域损失：融合图趋近可见光图像
        loss_background = torch.mean(background_mask * torch.abs(fused_img - vis_img))

        return loss_target + loss_background
```

### 2. 新增多任务联合损失函数

```python
class MultiTaskLoss(nn.Module):
    """多任务联合优化损失 - 论文3.5节

    整合SSIM损失、梯度损失、色彩损失和语义损失
    """
    def __init__(self):
        super(MultiTaskLoss, self).__init__()
        self.ssim_loss = L_SSIM()
        self.grad_loss = L_Grad_position()
        self.color_loss = L_color()
        self.masked_semantic_loss = MaskedSemanticLoss()

    def forward(self, fused, ir, vis, mask=None):
        # SSIM损失 - 结构保持
        loss_ssim = self.ssim_loss(vis, fused) + self.ssim_loss(ir, fused)

        # 梯度损失 - 边缘保持
        loss_grad = self.grad_loss(vis, ir, fused)

        # 色彩损失 - 色彩保真
        loss_color = self.color_loss(vis, fused)

        total = loss_ssim + loss_grad + loss_color

        # 语义损失（如果有mask）
        if mask is not None:
            loss_semantic = self.masked_semantic_loss(fused, ir, vis, mask)
            total = total + loss_semantic

        return total
```

## 三、损失函数权重设置（论文4.1.3节）

根据论文，训练参数设置：
- 批次大小 (Batch Size): 8
- 迭代轮数 (Epochs): 120
- 初始学习率: 1e-4
- 优化器: AdamW (weight_decay=0.05)
- 学习率调度: 余弦退火 + Warm-up

不同任务的损失权重：
| 任务类型 | max_ratio | ssim_ratio | text_ratio |
|---------|-----------|------------|------------|
| low_light (低光) | 8 | 1 | 10 |
| over_exposure (过曝) | 4 | 0 | 2 |
| ir_low_contrast (红外低对比度) | 8 | 1 | 10 |
| ir_noise (红外噪声) | 6 | 1 | 10 |

## 四、数据集格式

训练需要的数据结构（带语义分割mask）：

```
dataset/
    MSRS/
        train/
            Infrared/          # 红外图像
            Infrared_gt/       # 红外GT（可选）
            Visible/           # 可见光图像
            Visible_gt/        # 可见光GT（可选）
            mask/              # 语义分割掩码
                # 行人、车辆等目标的二值化掩码
            text/              # 文本提示
        eval/
            Infrared/
            Visible/
```

## 五、训练脚本修改 (train_fusion.py)

主要修改点：
1. 数据加载加入mask
2. 使用新的MultiTaskLoss
3. 支持不同任务的文本提示

## 六、验证指标

论文使用的评价指标：
- **EN** (Entropy) - 信息熵
- **SD** (Standard Deviation) - 标准差
- **SCD** (Sum of Correlations of Differences) - 相关差异和
- **VIF** (Visual Information Fidelity) - 视觉保真度
- **SSIM** - 结构相似度

## 七、下游任务验证

论文还提到在目标检测和语义分割任务上验证融合效果，可选做。

---

## 实施优先级

1. **高优先级**：新增MaskedSemanticLoss和MultiTaskLoss
2. **中优先级**：修改train_fusion.py支持mask训练
3. **低优先级**：准备带mask的数据集

